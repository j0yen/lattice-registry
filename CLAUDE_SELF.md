<!-- changelog: 2026-06-16 (build): shipped lattice-seed v0.1.0 — BFO/RO/IAO/COB/SWO ontology seeding pipeline -->
# lattice-registry — Claude Self Notes

## Changelog

- **2026-06-16**: shipped `lattice-seed` v0.1.0 — shell script to ingest BFO/RO/IAO/COB/SWO ontologies via `lattice-registry add`, bridge them pairwise against BFO/IAO, and join into the federated store at `~/.cache/lattice/store/`. Installed to `~/.local/bin/lattice-seed` and `scripts/lattice-seed.sh`.
